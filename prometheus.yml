global:
  scrape_interval: 15s # How often to check the API

scrape_configs:
  - job_name: 'fastapi-monitor'
    static_configs:
      # Use 'host.docker.internal' if running API locally, 
      # or the service name if running in Docker
      - targets: ['host.docker.internal:8000']